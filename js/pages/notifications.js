// PropOS — notifications page (placeholder until its phase)
export function render() {
  const el = document.getElementById('page-notifications');
  if (!el) return;
  // Content will be filled in a future phase
}
