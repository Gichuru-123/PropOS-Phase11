// PropOS — activity page (placeholder until its phase)
export function render() {
  const el = document.getElementById('page-activity');
  if (!el) return;
  // Content will be filled in a future phase
}
